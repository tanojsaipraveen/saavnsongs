import { Strformat2Pipe } from './strformat2.pipe';

describe('Strformat2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Strformat2Pipe();
    expect(pipe).toBeTruthy();
  });
});
